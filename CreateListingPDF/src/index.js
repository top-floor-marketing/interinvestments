import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("create_listing_pdf"));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

